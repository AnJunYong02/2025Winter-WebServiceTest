<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="User">
    <!-- 로그인 처리 sql문-->
    <select id="getUser" resultType="UserVO">
        SELECT userId, userPw FROM USER WHERE userId = #{userId} AND userPw = #{userPw}
    </select>

    <select id="insertUser" resultType="UserVO">
        INSERT INTO USER (userId, userName, concertName, concertDate, seatNum)
        VALUES (#{userId}, #{userName}, #{concertName}, #{concertDate}, #{seatNum});
    </select>
</mapper>